package p1;

/**
 * Enum representing the various states a system can be in.
 */
public enum SystemStatus {
    // State representing that the system has been initialized, is operational, is deadlocked, or has finished its operations.
}